name: Build Windows Executable

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
          
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build executable
      run: |
        pyinstaller --onefile --windowed --name="ExcelMergerTool" merge_excel_file.py
        
    - name: Upload Windows executable
      uses: actions/upload-artifact@v4
      with:
        name: ExcelMergerTool-Windows
        path: dist/ExcelMergerTool.exe
        retention-days: 30
        
    - name: Create release assets
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      run: |
        # 创建发布包
        mkdir release
        copy "dist\ExcelMergerTool.exe" release\
        copy README.md release\
        copy WINDOWS_BUILD_GUIDE.md release\
        
        # 创建ZIP包
        powershell Compress-Archive -Path release\* -DestinationPath "ExcelMergerTool-Windows.zip"
        
    - name: Upload release package
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: actions/upload-artifact@v4
      with:
        name: ExcelMergerTool-Complete-Package
        path: ExcelMergerTool-Windows.zip
        retention-days: 90

  build-macos:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
          
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build executable
      run: |
        pyinstaller --onefile --windowed --name="ExcelMergerTool" merge_excel_file.py
        
    - name: Upload macOS executable
      uses: actions/upload-artifact@v4
      with:
        name: ExcelMergerTool-macOS
        path: dist/ExcelMergerTool
        retention-days: 30
        
    - name: Upload macOS app
      uses: actions/upload-artifact@v4
      with:
        name: ExcelMergerTool-macOS-App
        path: dist/ExcelMergerTool.app
        retention-days: 30

  build-linux:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
          
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build executable
      run: |
        pyinstaller --onefile --windowed --name="ExcelMergerTool" merge_excel_file.py
        
    - name: Upload Linux executable
      uses: actions/upload-artifact@v4
      with:
        name: ExcelMergerTool-Linux
        path: dist/ExcelMergerTool
        retention-days: 30 
